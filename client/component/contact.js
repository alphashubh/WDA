import React from 'react';

import Header from './header';

const Contact = (props) => {
    return(
        <div className="main">
                <h1>Contact..{props.name}</h1>
        </div>
    )
}

export default Contact;